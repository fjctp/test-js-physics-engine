# test-js-physics-engine

Try out physics engine, `matter.js`, written in JS.

## Start a dev server

```bash
# Start a dev server.
deno task dev

# Open the browser to the local web server.
xdg-open http://localhost:8000
```
